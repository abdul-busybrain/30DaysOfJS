// level 2
webTechs = [
  "JavaScript, HTML, CSS, React, Bootstrap, jQuery, Node.js, Python (Django), PHP (Laravel), Git",
];

if (webTechs.includes("Sass")) {
  console.log("Okay");
} else {
  webTechs.push("Sass");
}

console.log(webTechs);
